export const holder = null

export const defaultImage = {
  full: holder,
  high: holder,
  medium: holder,
  thumb: holder
}

export const defaultImages = {
  backdrop: defaultImage,
  poster: defaultImage,
  logo: defaultImage,
  banner: defaultImage,
}

export const defaultShowImages = {
  backdrop: defaultImage,
  poster: defaultImage,
  // logo: defaultImage,
  // banner: defaultImage,
}

export const defaultMovieImages = defaultShowImages

export const defaultEpisodeImages = defaultImages

export const defaultSeasonImages = defaultImages
